server:
  port: 8800

spring:
  application:
    name: elasticsearch
  cloud:
    config:
      uri: config

ribbon:
  eureka:
    enabled: true

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8760/eureka/