allprojects {
    group 'org.infrastruction'
    version currentVersion
}

subprojects {
    apply plugin: 'java'

    repositories {
        mavenCentral()

        maven { url 'https://repo.spring.io/libs-milestone' }
    }
}
